const styles = {
  listView: {
    maxWidth: "100%",
    overflow: "auto hidden",
  },

  innerListView: {
    display: "flex",
    alignItems: "center",
    width: "max-content",
    transition: "transform .8s",
  },

  innerListViewMargin: {
    flexShrink: 0,
  },
};

export default styles;
