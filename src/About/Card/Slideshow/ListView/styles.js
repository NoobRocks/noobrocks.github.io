const styles = {
  listView: {
    maxWidth: "100%",
    overflow: "hidden",
  },

  innerListView: {
    display: "flex",
    alignItems: "center",
    width: "max-content",
  },

  innerListViewMargin: {
    flexShrink: 0,
  },
};

export default styles;
